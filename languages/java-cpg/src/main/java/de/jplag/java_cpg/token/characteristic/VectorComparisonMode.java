package de.jplag.java_cpg.token.characteristic;

/**
 * An enum representing the different modes to compare characteristic vectors.
 */
public enum VectorComparisonMode {
    EUCLIDEAN_DISTANCE,
    COSINE_SIMILARITY,
    L1_DISTANCE
}
