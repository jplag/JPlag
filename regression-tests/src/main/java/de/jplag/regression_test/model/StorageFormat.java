package de.jplag.regression_test.model;

/**
 * Enum representing storage formats for datasets or files.
 */
public enum StorageFormat {
    /** Storage as a directory in the file system. */
    DIRECTORY,
    /** Storage as a compressed ZIP archive. */
    ZIP
}
